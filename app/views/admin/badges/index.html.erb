<%= render 'shared/errors', resource: @test %>

<table class="table table-bordered table-hover text-center">
  <tr>
    <th class="sort-by-title pointer">
      <%= t('.title') %>
      </th>
    <th><%= t('.img_address') %></th>
    <th><%= t('.rule') %></th>
    <th><%= t('.rule_value') %></th>
    <th><%= t('.author') %></th>
    <th colspan="3"><%= t('.action') %></th>
  </tr>

  <%= render Badge.all %>

</table>
<p>
  <%= link_to t('.new_badge'), new_admin_badge_path, class: "btn btn-info" %>
</p>
